import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    success: {
      main: "#4caf50",
    },
    primary: {
      main: "#2196f3",
    },
  },
});

export default theme;
